function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  //This is where we will paste all the code stuff we do from p5 so that everyone has the same copy
  //Click on the pencil on the right corner to edit the code.
}
